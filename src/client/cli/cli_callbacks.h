#ifndef CLI_CALLBACKS_H
#define CLI_CALLBACKS_H

#include "client_api.h"

/* Returns a pointer to the CLI callbacks structure */
ClientCallbacks* cli_get_callbacks(void);

#endif /* CLI_CALLBACKS_H */
